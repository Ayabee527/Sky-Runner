package;

import flixel.FlxSprite;

class Player extends FlxSprite
{
	override public function new(x:Float, y:Float = 0)
	{
		super(x, y);
	}

	override function update(elapsed:Float)
	{
		super.update(elapsed);
	}
}
